# lite-cdn
A lightweight, single-purpose image hosting and routing utility built to manage custom domain assets and redirect services without the overhead of external dependencies. Designed for fast deployment and minimal resource usage, it handles subdomain-based routing and automatic Discord/Twitter metadata injection for shared assets.
